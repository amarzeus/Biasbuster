{
  "name": "biasbuster-web-platform",
  "version": "1.0.0",
  "scripts": {
    "dev": "live-server --port=3000 --no-browser",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.0-beta.3",
    "@testing-library/jest-dom": "^6.6.3"
  }
}
